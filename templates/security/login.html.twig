<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <title>{% block title %}Portail des compétences {% endblock %}</title>

    {% block stylesheets %}
        {{ encore_entry_link_tags('app') }}
    {% endblock %}

    {% block javascripts %}
        {#{{ encore_entry_script_tags('app') }}#}
    {% endblock %}
</head>

<body>
{% block body %}
    <div class="py-5">
    <form class="border shadow border-3 border-warning rounded-3 mt-5 ms-auto me-auto w-50 px-5 py-4" method="post">
        {% if error %}
            <div class="alert alert-danger">{{ error.messageKey|trans(error.messageData, 'security') }}</div>
        {% endif %}

        <h3 class="mb-3 font-weight-normal">IDENTIFICATION</h3>
    <div class="ps-5">
        <label class="ms-5" for="inputEmail">Email</label>
        <input type="email"  name="email" id="inputEmail" class="form-control mb-5 shadow-sm ms-5 w-75" autocomplete="email" required autofocus>
        <label class="ms-5" for="inputPassword">Password</label>
        <input type="password" name="password" id="inputPassword" class="form-control mb-5 shadow-sm ms-5 w-75" autocomplete="current-password" required>
    </div>
        <input type="hidden" name="_csrf_token"
               value="{{ csrf_token('authenticate') }}"
        >


        {#
            Uncomment this section and add a remember_me option below your firewall to activate remember me functionality.
            See https://symfony.com/doc/current/security/remember_me.html

            <div class="checkbox mb-3">
                <label>
                    <input type="checkbox" name="_remember_me"> Remember me
                </label>
            </div>
        #}


        <button class="btn btn-warning d-flex ms-auto me-auto shadow-sm" type="submit">
            CONNEXION
        </button>
    </form>
    </div>
{% endblock %}
</body>
</html>